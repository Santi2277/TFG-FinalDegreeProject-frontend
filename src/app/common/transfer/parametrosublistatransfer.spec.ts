import { Parametrosublistatransfer } from './parametrosublistatransfer';

describe('Parametrosublistatransfer', () => {
  it('should create an instance', () => {
    expect(new Parametrosublistatransfer()).toBeTruthy();
  });
});
