import { Parametrosublistaedit } from './parametrosublistaedit';

describe('Parametrosublistaedit', () => {
  it('should create an instance', () => {
    expect(new Parametrosublistaedit()).toBeTruthy();
  });
});
