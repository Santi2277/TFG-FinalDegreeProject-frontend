import { Parametrolistaedit } from './parametrolistaedit';

describe('Parametrolistaedit', () => {
  it('should create an instance', () => {
    expect(new Parametrolistaedit()).toBeTruthy();
  });
});
